type TOC = {
  currentLink: string;
  vanillatitle: string;
};

type BookInfo = {
  bookname: string;
  author?: string;
  description?: string;
  disable?: boolean;
  cover?: string;
  version?: string;
};
